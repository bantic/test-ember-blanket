/* globals blanket, module */

var options = {
  modulePrefix: 'test-ember-blanket',
  filter: '//.*test-ember-blanket/.*/',
  antifilter: '//.*(tests|template).*/',
  loaderExclusions: [],
  enableCoverage: true,
  cliOptions: {
    reporters: ['json'],
    autostart: true
  }
};
if (typeof exports === 'undefined') {
  blanket.options(options);
} else {
  module.exports = options;
}
